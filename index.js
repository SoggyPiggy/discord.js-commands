const { Client } = require('./src/Client');
const { Command } = require('./src/structures/Command');
const { CommandGroup } = require('./src/structures/CommandGroup');
const { ParameterType } = require('./src/structures/ParameterType');

module.exports = {
  Client,
  Command,
  CommandGroup,
  ParameterType,
};
